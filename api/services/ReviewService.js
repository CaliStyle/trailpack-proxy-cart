'use strict'

const Service = require('trails/service')

/**
 * @module ReviewService
 * @description Review Service
 */
module.exports = class ReviewService extends Service {
  // TODO
  create(review) {
    return Promise.resolve(review)
  }
  // TODO
  update(review) {
    return Promise.resolve(review)
  }
  // TODO
  destroy(review) {
    return Promise.resolve(review)
  }
}


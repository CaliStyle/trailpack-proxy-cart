'use strict'
/* global describe, it */
const assert = require('assert')

describe('CollectionCsvService', () => {
  it('should exist', () => {
    assert(global.app.api.services['CollectionCsvService'])
  })
})

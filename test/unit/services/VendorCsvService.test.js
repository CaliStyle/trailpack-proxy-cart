'use strict'
/* global describe, it */
const assert = require('assert')

describe('VendorCsvService', () => {
  it('should exist', () => {
    assert(global.app.api.services['VendorCsvService'])
  })
})

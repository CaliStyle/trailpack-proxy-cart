'use strict'
/* global describe, it */
const assert = require('assert')

describe('CustomerCsvService', () => {
  it('should exist', () => {
    assert(global.app.api.services['CustomerCsvService'])
  })
})

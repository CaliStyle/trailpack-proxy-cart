'use strict'
/* global describe, it */
const assert = require('assert')

describe('SubscriptionCsvService', () => {
  it('should exist', () => {
    assert(global.app.api.services['SubscriptionCsvService'])
  })
})

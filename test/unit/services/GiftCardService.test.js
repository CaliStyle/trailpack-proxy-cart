'use strict'
/* global describe, it */
const assert = require('assert')

describe('GiftCardService', () => {
  it('should exist', () => {
    assert(global.app.api.services['GiftCardService'])
  })
})

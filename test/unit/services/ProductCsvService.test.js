'use strict'
/* global describe, it */
const assert = require('assert')

describe('ProductCsvService', () => {
  it('should exist', () => {
    assert(global.app.api.services['ProductCsvService'])
  })
})

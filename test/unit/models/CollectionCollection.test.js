'use strict'
/* global describe, it */
const assert = require('assert')

describe('CollectionCollection Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['CollectionCollection'])
  })
})

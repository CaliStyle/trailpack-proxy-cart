'use strict'
/* global describe, it */
const assert = require('assert')

describe('ProductAssociationUpload Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ProductAssociationUpload'])
  })
})

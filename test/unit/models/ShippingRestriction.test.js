'use strict'
/* global describe, it */
const assert = require('assert')

describe('ShippingRestriction Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ShippingRestriction'])
  })
})

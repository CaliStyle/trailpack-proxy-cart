'use strict'
/* global describe, it */
const assert = require('assert')

describe('ProductMetaUpload Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ProductMetaUpload'])
  })
})

'use strict'
/* global describe, it */
const assert = require('assert')

describe('VendorProduct Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['VendorProduct'])
  })
})

'use strict'
/* global describe, it */
const assert = require('assert')

describe('ProductVariant Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ProductVariant'])
  })
})

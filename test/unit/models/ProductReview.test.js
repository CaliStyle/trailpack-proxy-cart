'use strict'
/* global describe, it */
const assert = require('assert')

describe('ProductReview Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ProductReview'])
  })
})

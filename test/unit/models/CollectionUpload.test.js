'use strict'
/* global describe, it */
const assert = require('assert')

describe('CollectionUpload Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['CollectionUpload'])
  })
})

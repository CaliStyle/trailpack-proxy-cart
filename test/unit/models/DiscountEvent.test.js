'use strict'
/* global describe, it */
const assert = require('assert')

describe('DiscountEvent Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['DiscountEvent'])
  })
})

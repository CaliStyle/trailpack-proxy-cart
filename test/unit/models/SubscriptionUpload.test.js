'use strict'
/* global describe, it */
const assert = require('assert')

describe('SubscriptionUpload Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['SubscriptionUpload'])
  })
})

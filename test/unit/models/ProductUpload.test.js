'use strict'
/* global describe, it */
const assert = require('assert')

describe('ProductUpload Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ProductUpload'])
  })
})

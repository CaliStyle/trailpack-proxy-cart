'use strict'
/* global describe, it */
const assert = require('assert')

describe('ItemCollection Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ItemCollection'])
  })
})

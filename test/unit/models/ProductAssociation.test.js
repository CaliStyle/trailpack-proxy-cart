'use strict'
/* global describe, it */
const assert = require('assert')

describe('ProductAssociation Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ProductAssociation'])
  })
})

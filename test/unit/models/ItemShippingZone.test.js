'use strict'
/* global describe, it */
const assert = require('assert')

describe('ItemShippingZone Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ItemShippingZone'])
  })
})

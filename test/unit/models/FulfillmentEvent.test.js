'use strict'
/* global describe, it */
const assert = require('assert')

describe('FulfillmentEvent Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['FulfillmentEvent'])
  })
})

'use strict'
/* global describe, it */
const assert = require('assert')

describe('RefundOrderItem Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['RefundOrderItem'])
  })
})

'use strict'
/* global describe, it */
const assert = require('assert')

describe('TagController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['TagController'])
  })
})

'use strict'
/* global describe, it */
const assert = require('assert')

describe('CartController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['CartController'])
  })
})

'use strict'
/* global describe, it */
const assert = require('assert')

describe('ShopController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ShopController'])
  })
})
